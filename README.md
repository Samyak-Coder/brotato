# Yo

I have defined types arbitarily (mostly form the video), they can be changed.
as of now (11/06) the walls (4 rects) and their animated view would not change, the resolveWallCollision(logic.ts) and useFrameCallBack(wall.tsx)'s code would remain until some better aproach is found.

wrote the singular functions myslef, when I had to scale them to multiple enemies for loops and shit used claude.